
# Use SQLite by default (no Postgres needed)
# DATABASE_URL=sqlite:///./vendnest.db

# Or Postgres (used by Docker):
# DATABASE_URL=postgresql+psycopg2://vendnest:vendnest@localhost:5432/vendnest

JWT_SECRET=dev-secret-change-me
JWT_TTL_MIN=120
